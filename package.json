{
  "name": "test-extension",
  "version": "0.1.0",
  "description": "A web extension",
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup -c -w && web-ext run --source-dir dist",
    "prepare": "npm run build"
  },
  "author": "geospiza <geospiza@pm.me>",
  "license": "MIT",
  "devDependencies": {
    "rollup": "^2.38.4",
    "rollup-plugin-copy": "^3.3.0",
    "web-ext": "^5.5.0"
  },
  "dependencies": {}
}
